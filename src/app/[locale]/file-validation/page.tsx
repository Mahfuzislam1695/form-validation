import FileValidation from '@/components/fileValidation/FileValidation';
import React from 'react';

const page = () => {
    return (
        <div>
            <FileValidation/>
        </div>
    );
};

export default page;